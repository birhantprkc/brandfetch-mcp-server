{
  "name": "@brandfetch/mcp-server-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP Apps brand-card UI for the Brandfetch MCP server. Builds a single-file HTML artifact into ../src/ui/.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
